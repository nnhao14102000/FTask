name: FTask of branch hao

on:
  push:
    branch:
      - hao

env: 
  DOTNET_VERSION: '5.0.x'
  AZURE_WEBAPP_NAME: FTask

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code 🛎
        uses: actions/checkout@v2

      - name: Setup .NET 5 🔧
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Start SQL Server 
        run: |
          docker run -d -e 'ACCEPT_EULA=Y' -e 'SA_PASSWORD=FTaskPa55w0rd!' -e 'MSSQL_PID=Express' -p 1433:1433 -d mcr.microsoft.com/mssql/server:latest

      - name: Sleep for 5 seconds 🤣
        uses: jakejarvis/wait-action@master
        with:
          time: '5s'

      - name: Restore dependencies 🛠
        run: dotnet restore

      - name: Build app 🚀
        env: 
          ConnectionStrings__DefaultConnection: Server=localhost;Database=FTask;User=sa;Password=FTaskPa55w0rd!
          ConnectionStrings__AuthConnection: Server=localhost;Database=FTaskAuthDB;User=sa;Password=FTaskPa55w0rd!
        run: dotnet build -c Release --no-restore

      - name: Publish app 📰
        env: 
          ConnectionStrings__DefaultConnection: Server=localhost;Database=FTask;User=sa;Password=FTaskPa55w0rd!
          ConnectionStrings__AuthConnection: Server=localhost;Database=FTaskAuthDB;User=sa;Password=FTaskPa55w0rd!
        run: dotnet build -c Release -o ./out 

      - name: Deploy to Azure Web Apps 🚢
        uses: azure/webapps-deploy@v2
        with: 
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_SECRET }}
          package: ./out
